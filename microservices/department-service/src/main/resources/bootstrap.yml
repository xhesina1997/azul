server:
  port: 8091

spring:
  application:
    name: department-service
  cloud:
    config:
      uri: http://localhost:8088
      fail-fast: true
      retry:
        max-attempts: 3600
        initial-interval: 5000