server:
  port: 8091
spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 3600
        initial-interval: 5000