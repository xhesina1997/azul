server:
  port: 9100

spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8088
      fail-fast: true
      retry:
        max-attempts: 3600
        initial-interval: 5000