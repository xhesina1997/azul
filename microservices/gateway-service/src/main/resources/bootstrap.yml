
server:
  port: 8060

spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://localhost:8088
      fail-fast: true
      retry:
        max-attempts: 3600
        initial-interval: 5000
