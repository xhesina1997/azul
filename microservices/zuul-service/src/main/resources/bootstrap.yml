server:
  port: 8762

spring:
  application:
    name: zuul-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 3600
        initial-interval: 5000