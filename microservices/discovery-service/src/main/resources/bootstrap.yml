server:
  port: 8061
spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://localhost:8088
      fail-fast: true
      retry:
        max-attempts: 3600
        initial-interval: 5000

