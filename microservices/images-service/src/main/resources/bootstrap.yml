server:
  port: 9090

spring:
  application:
    name: images-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 3600
        initial-interval: 5000